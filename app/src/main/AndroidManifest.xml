<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wordify_00009987">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Wordify_00009987">
        <activity
            android:name=".WordItemActivity"
            android:exported="true" />
        <activity
            android:name=".DetailedWordActivity"
            android:exported="true"
            android:label="word details" />
        <activity
            android:name=".WordsListActivity"
            android:exported="true"
            android:label="@string/words" />
        <activity
            android:name=".NewWordActivity"
            android:exported="true"
            android:label="new word" />
        <activity
            android:name=".AboutActivity"
            android:exported="true"
            android:label="@string/about" />
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="home"
            android:launchMode="singleTask"/>
    </application>

</manifest>